apiVersion: v1
kind: Service
metadata:
  name: nextjs-jwt-auth-app-service
  namespace: da
  labels:
    app: nextjs-jwt-auth-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: nextjs-jwt-auth-app
