apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nextjs-jwt-auth-app-ingress
  namespace: da
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nextjs-jwt-auth-app-service
            port:
              number: 80
